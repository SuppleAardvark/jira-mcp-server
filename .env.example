# Required
JIRA_BASE_URL=https://your-domain.atlassian.net
JIRA_EMAIL=your-email@example.com
JIRA_API_TOKEN=your-api-token

# Optional: Restrict available tools (comma-separated)
# JIRA_SCOPES=boards:read,sprints:read,issues:read

# Optional: Restrict to specific boards (pipe-separated, by ID or name)
# JIRA_ALLOWED_BOARDS=123|My Board

# Optional: Restrict to specific projects (pipe-separated)
# JIRA_ALLOWED_PROJECTS=PROJ|DEV

# Optional: Restrict to specific issue types (pipe-separated)
# JIRA_ALLOWED_ISSUE_TYPES=Bug|Task|Story

# Optional: Sprint Report Configuration
# Story points custom field ID (use jira_get_field_schema to find this)
# JIRA_STORY_POINTS_FIELD=customfield_10024

# Status groups for sprint report categorization
# Format: GroupName:Status1,Status2|GroupName2:Status3,Status4
# JIRA_STATUS_GROUPS=To Do:To Do,Open|Blocked:Blocked,Blocked on QA|In Progress:In Progress|Done:Done,Closed

# Statuses that count as "open bugs in backlog" (pipe-separated)
# If set, only bugs with these statuses are counted in bug backlog
# JIRA_BUG_BACKLOG_STATUSES=To Do

# Statuses that count as "blocked" (pipe-separated)
# JIRA_BLOCKED_STATUSES=Blocked|Blocked on QA

# Statuses that count as "done" (pipe-separated)
# Used for filtering out completed issues from backlog counts
# JIRA_DONE_STATUSES=Done|Closed|Invalid|Parked|Resolved
